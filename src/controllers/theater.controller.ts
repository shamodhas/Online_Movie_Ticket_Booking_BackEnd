import express from "express";

export const getAllTheaters = (
  req: express.Request,
  res: express.Response
) => {};

export const getTheaterByName = (
  req: express.Request,
  res: express.Response
) => {};

export const getTheatersByUser = (
  req: express.Request,
  res: express.Response
) => {};

export const saveTheater = (
  req: express.Request,
  res: express.Response
) => {};

export const updateTheater = (
  req: express.Request,
  res: express.Response
) => {};

export const deleteTheater = (
  req: express.Request,
  res: express.Response
) => {};
